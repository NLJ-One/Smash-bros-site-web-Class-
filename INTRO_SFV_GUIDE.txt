## 🎮 STREET FIGHTER V - ANIMATION D'ENTRÉE - FLUX COMPLET

### 📊 Flux de navigation

```
┌─────────────────────────────┐
│  1. fighter-select.html     │
│  - Grille de sélection      │
│  - Hover + glow             │
│  - Click COMPARE            │
└────────────┬────────────────┘
             │
             ↓
     [Screen Flash Animation]
             │
             ↓
┌─────────────────────────────┐
│  2. intro-sfv.html          │
│  ★ NEW - ANIMATION D'ENTRÉE │
│  - Portrait gauche glisse   │
│  - Portrait droite glisse   │
│  - VS Flash au centre       │
│  - Noms apparaissent        │
│  - Durée: ~2 secondes       │
└────────────┬────────────────┘
             │
             ↓ (Auto-redirect)
┌─────────────────────────────┐
│  3. compare.html            │
│  - Paramètres URL           │
│  - Pré-sélection auto       │
│  - Comparaison affichée     │
└─────────────────────────────┘
```

---

### ⚡ Timing des animations

```
INTRO-SFV.HTML Timeline:
───────────────────────────────────

0.0s - Page charge
       Background pulse démarre

0.0s - Portraits démarrent animations
       Left: slideLeft (0.9s)
       Right: slideRight (0.9s)

0.6s - VS Flash démarre
       vsImpact (0.9s)
       Animation delay: 0.6s

1.2s - Noms apparaissent
       Fade in avec slideUp
       Animation delay: 1.2s

1.5s - Flash blanc final
       Transition effect

2.0s - Redirection vers compare.html
       Auto-redirect avec paramètres
```

---

### 🎨 Animations CSS implémentées

#### 1. **introSlideLeft**
```css
- Glisse depuis left: -50% vers left: 10%
- Scale: 1.3 → 1.05 → 1
- Rotation: rotateY(15deg) → rotateY(0deg)
- Durée: 0.9s
- Easing: cubic-bezier(0.34, 1.56, 0.64, 1) [bounce]
```

#### 2. **introSlideRight**
```css
- Glisse depuis right: -50% vers right: 10%
- Scale: 1.3 → 1.05 → 1
- Rotation: rotateY(-15deg) → rotateY(0deg)
- Durée: 0.9s
- Easing: cubic-bezier(0.34, 1.56, 0.64, 1) [bounce]
```

#### 3. **introVSImpact**
```css
- Scale: 0.3 → 1.5 → 1.1 → 1
- Opacity: 0 → 1 → 0.95
- Blur: 10px → 0 → 0
- Durée: 0.9s
- Delay: 0.6s
- Easing: cubic-bezier bounce
```

#### 4. **introNameAppear**
```css
- Fade in + slide up
- Opacity: 0 → 1
- Transform: translateY(20px) → translateY(0)
- Durée: 0.6s
- Delay: 1.2s
```

#### 5. **introFlash**
```css
- Flash blanc rapide
- Opacity: 0.8 → 0
- Durée: 0.3s
- Delay: 1.3s
```

#### 6. **introBgPulse**
```css
- Background pulsation
- Opacity: 0 → 0.5 → 0.5 → 0
- Durée: 3s
```

---

### 📱 Responsive

| Écran | Portrait Width | VS Size | Ajustements |
|-------|---|---|---|
| Desktop (1920px) | 35% / 400px | 12rem | Layout normal |
| Tablette (768px) | 35% / 400px | 8rem | Réduit |
| Mobile (480px) | 50% / 150px | 6rem | Positions ajustées |

---

### 🔧 Paramètres URL

**Format:**
```
intro-sfv.html?left=Ryu&right=Ken&leftImg=...&rightImg=...
```

**Paramètres:**
- `left` : Nom du combattant gauche (encodé)
- `right` : Nom du combattant droit (encodé)
- `leftImg` : URL image gauche (optionnel, encodée)
- `rightImg` : URL image droite (optionnel, encodée)

**Exemple complet:**
```
intro-sfv.html?left=Ryu&right=Ken&leftImg=../images/ryu.png&rightImg=../images/ken.png
```

---

### 🎮 API JavaScript

#### Depuis fighter-select.js:
```javascript
// Redirection vers intro
globalThis.location.href = `intro-sfv.html?${params.toString()}`;
```

#### Depuis intro-sfv.js:
```javascript
// Redirection vers compare
globalThis.location.href = `compare.html?left=${encodeURIComponent(leftFighter)}&right=${encodeURIComponent(rightFighter)}`;
```

#### API publique:
```javascript
// Accessible depuis console ou autres scripts
globalThis.introSFV.play(leftName, rightName, leftImg, rightImg);
```

---

### 🎬 Exemple d'utilisation complète

```javascript
// Depuis fighter-select.html
function handleCompare() {
    // Récupérer les données sélectionnées
    const leftName = selectedLeft.dataset.name;
    const rightName = selectedRight.dataset.name;
    const leftImg = document.getElementById('selectedLeftImg').src;
    const rightImg = document.getElementById('selectedRightImg').src;

    // Créer les paramètres
    const params = new URLSearchParams();
    params.append('left', leftName);
    params.append('right', rightName);
    params.append('leftImg', leftImg);
    params.append('rightImg', rightImg);

    // Redirection vers l'intro
    globalThis.location.href = `intro-sfv.html?${params.toString()}`;
}
```

---

### ✅ Checklist

- [x] Page intro-sfv.html créée
- [x] CSS animations inline (performance++)
- [x] JavaScript logique d'animation
- [x] Intégration fighter-select.js
- [x] Paramètres URL
- [x] Redirection auto vers compare.html
- [x] Responsive design
- [x] Pas d'erreurs JavaScript
- [x] Sécurité (encodeURIComponent, globalThis)

---

### 🚀 Prêt pour production !

Le système complet:
1. **fighter-select.html** → Sélection
2. **intro-sfv.html** → Animation d'entrée ⭐ NEW
3. **compare.html** → Comparaison

Flux complètement intégré et fluide ! 🎮✨
